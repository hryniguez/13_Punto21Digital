<!--ACA SE INCLUYE EL PARTIAL : HEAD -->
<%- include("../partials/head.ejs") %>

<body class="body_home">

  <!--ACA SE INCLUYE EL PARTIAL : HEADER -->
  <%- include("../partials/header.ejs") %>

    <main class="main__form main__formProfile">
      <div class="main__title__box">
        <hr class="main__form__lines">
        <h2 class="main__form__title">CREAR ADMIN</h2>
        <hr class="main__form__lines">
      </div>
      <form action="/users/createPrivileges" method="post" class="main__form__register" enctype="multipart/form-data">
       
       
              
        <label for="name">NOMBRE</label>
        <input type="text" name="name" placeholder="Nombre y Apellido" id="name" value="<%= locals.old && old.name ? old.name : null %>" >
        <p><%= locals.errores && errores.name ? errores.name.msg : null %> </p>

        <label for="email">EMAIL</label>
        <input type="email" name="email" placeholder="Ingresa tu email" value="<%= locals.old && old.email ? old.email : null %>" >
        <p><%= locals.errores && errores.email ? errores.email.msg : null %> </p>

        <label for="password">CONTRASEÑA</label>
        <div class="main__form__register__password">
          <input type="password" name="password" class="main__form__register__password" value="<%= locals.old && old.password ? old.name : null %>" >
          <p><%= locals.errores && errores.password ? errores.password.msg : null %> </p>
         
        </div>

            <div>
            <label for="category" >CATEGORIA :</label>
              <select name="category" id="category" value="">
                <option value="USUARIO" >USER</option>
                <option value="ADMIN" >ADMIN</option>
              </select>
          </div>
          <label for="image">SELECCIONAR AVATAR: </label>
          <div class="img_create_fast">
              
            <input class="dashboard__image" name="image" type="file" accept="image/*" >
           
          </div>


<!-- 
        <label for="image">AVATAR : </label>
      <input type="file" name="image" id="image" accept="image/*" >
      <p><%= locals.errores && errores.image ? errores.image.msg : null %> </p>
        <div class="main__form__register__box">
 -->
          
          <button class="main__form__button" type="submit">CREAR CUENTA DE USUARIO</button>
        </div>
      </form>
    </main>

<!--ACA SE INCLUYE EL PARTIAL : -->
<%- include("../partials/footer.ejs") %>


</body>

</html>